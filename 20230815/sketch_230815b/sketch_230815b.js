function setup() {
  //createCanvas( 300, 200 );
  //createCanvas( windowWidth, windowHeight );
  createCanvas( 400, 400 );
  
}


function draw() {
  background(255, 200, 200);
  fill(0);
  textSize(40);
  textAlign(CENTER, CENTER);
  text("Soy P5*js", mouseX,mouseY)
}
